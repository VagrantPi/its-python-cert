# ITS Python 證照｜模擬題庫（Q26–Q50，進階補充卷）

> 承接 `03-模擬題與解析.md` 的 Q1–Q25，本卷為 Q26–Q50，題庫合計 **50 題**。所有含程式輸出的題目均經**實跑 Python 驗證**。
> 領域配比：領域1（Q26–31）｜領域2（Q32–38）｜領域3（Q39–43）｜領域4（Q44–46）｜領域5（Q47–48）｜領域6（Q49–50）

---

## 領域 1：資料型別與運算子（Q26–Q31）

**Q26.** `print(10 % 3 + 2 * 2)` → ?　(A) `5` (B) `9` (C) `6` (D) `1`
**Q27.** `print(bool(0), bool(""), bool("0"))` → ?　(A) `False False False` (B) `False False True` (C) `True True True` (D) `False True True`
**Q28.** `x = [1, 2, 3]; print(len(x), x[-1])` → ?　(A) `3 3` (B) `3 1` (C) `2 3` (D) `3 -1`
**Q29.** `print("Hello".upper())` → ?　(A) `hello` (B) `HELLO` (C) `Hello` (D) `HELLO()`
**Q30.** `print(3 == 3.0)` → ?　(A) `True` (B) `False` (C) `錯誤` (D) `None`
**Q31.** `print("abc"[::-1])` → ?　(A) `abc` (B) `cba` (C) `cb` (D) `錯誤`

## 領域 2：流程控制（Q32–Q38）

**Q32.**
```python
n = 4
if n % 2 == 0 and n > 3:
    print("yes")
else:
    print("no")
```
→ ?　(A) `yes` (B) `no` (C) 無輸出 (D) 錯誤

**Q33.** `print(sum(range(1, 5)))` → ?　(A) `15` (B) `10` (C) `5` (D) `4`

**Q34.**
```python
result = ""
for i in range(1, 4):
    for j in range(i):
        result += "*"
print(result)
```
→ ?　(A) `***` (B) `******`（6 個） (C) `*********`（9 個） (D) `*`

**Q35.**
```python
i = 5
while i > 0:
    print(i, end="")
    i -= 2
```
→ ?　(A) `531` (B) `54321` (C) `5432` (D) `543210`

**Q36.**
```python
for x in "abc":
    if x == "b":
        break
    print(x, end="")
```
→ ?　(A) `a` (B) `abc` (C) `ab` (D) `ac`

**Q37.** `y = 10; print("big" if y > 5 else "small")` → ?　(A) `small` (B) `big` (C) `10` (D) 錯誤

**Q38.**
```python
count = 0
for i in range(10):
    if i % 3 == 0:
        count += 1
print(count)
```
→ ?　(A) `3` (B) `4`（0,3,6,9） (C) `2` (D) `10`

## 領域 3：輸入與輸出（Q39–Q43）

**Q39.** 使用者輸入 `hello`，`print(len(input()))` → ?　(A) `4` (B) `5` (C) `hello` (D) 錯誤
**Q40.** `print("A", "B", sep="", end="!")` → ?　(A) `A B!` (B) `AB!` (C) `AB` (D) `A B !`
**Q41.** 哪個模式**唯讀**開檔？　(A) `"w"` (B) `"a"` (C) `"r"` (D) `"x"`
**Q42.** `f.readlines()` 回傳什麼？　(A) 單一字串 (B) **每行為元素的串列** (C) 整數 (D) 檔案物件
**Q43.** 輸入分別為 `3`、`4`，`print(int(input()) + int(input()))` → ?　(A) `34` (B) `7` (C) `12` (D) 錯誤

## 領域 4：函式與程式結構（Q44–Q46）

**Q44.**
```python
def add(a, b=5):
    return a + b
print(add(3), add(3, 10))
```
→ ?　(A) `8 13` (B) `8 8` (C) `3 10` (D) 錯誤

**Q45.**
```python
def total(*nums):
    return sum(nums)
print(total(1, 2, 3, 4))
```
→ ?　(A) `4` (B) `10` (C) `1234` (D) 錯誤

**Q46.** `def f(): pass` 後 `print(f())` → ?　(A) `pass` (B) `0` (C) `None` (D) 錯誤

## 領域 5：錯誤處理（Q47–Q48）

**Q47.**
```python
try:
    x = [1, 2, 3]
    print(x[5])
except IndexError:
    print("out of range")
```
→ ?　(A) `out of range` (B) `3` (C) 崩潰 (D) 無輸出

**Q48.** `d = {}; d["k"]` 會引發？　(A) `IndexError` (B) `ValueError` (C) `KeyError` (D) 無錯誤

## 領域 6：模組（Q49–Q50）

**Q49.** `import random` 後，`random.randint(1, 6)` 可能的最大值為？　(A) `5` (B) `6` (C) `7` (D) `1`
**Q50.**
```python
import math
print(math.pi > 3, math.ceil(4.1))
```
→ ?　(A) `True 4` (B) `False 5` (C) `True 5` (D) `True 4.1`

---

## 解析與答案

| 題 | 答 | 解析 |
|:--:|:--:|------|
| Q26 | A | `10%3=1`、`2*2=4`，`1+4=5`（`%` 與 `*` 同級，`+` 較後）。 |
| Q27 | B | `0`、空字串為假 → False；非空字串 `"0"` 為真 → True。 |
| Q28 | A | 長度 3；`x[-1]` 取最後元素 3。 |
| Q29 | B | `.upper()` 轉大寫。 |
| Q30 | A | `3 == 3.0` 數值相等為 True（int 與 float 可比較）。 |
| Q31 | B | `[::-1]` 反轉字串 → `cba`。 |
| Q32 | A | 4 是偶數且 >3，兩條件皆真 → `yes`。 |
| Q33 | B | `range(1,5)=1,2,3,4`，總和 10。 |
| Q34 | B | 1+2+3=6 個星號。 |
| Q35 | A | 5→3→1（每次 -2，>0 才印）→ `531`。 |
| Q36 | A | 遇 `"b"` 前只印了 `a` 就 break。 |
| Q37 | B | 三元運算：y>5 為真 → `big`。 |
| Q38 | B | 0,3,6,9 共 4 個（0 也可被 3 整除）。 |
| Q39 | B | `input()` 回傳 `"hello"`，長度 5。 |
| Q40 | B | `sep=""` 不分隔、`end="!"` 收尾 → `AB!`。 |
| Q41 | C | `"r"` 唯讀；`"w"` 覆寫、`"a"` 附加、`"x"` 建立新檔。 |
| Q42 | B | `readlines()` 回傳每行為元素的**串列**。 |
| Q43 | B | 兩次轉 int 相加 `3+4=7`。 |
| Q44 | A | `add(3)` 用預設 b=5 → 8；`add(3,10)` → 13。 |
| Q45 | B | `*nums` 收集成 tuple，`sum` = 10。 |
| Q46 | C | 函式無 return，預設回傳 `None`。 |
| Q47 | A | 索引 5 越界觸發 `IndexError`，被攔截。 |
| Q48 | C | 存取不存在的鍵 → `KeyError`。 |
| Q49 | B | `randint(a,b)` **含兩端**，最大為 6。 |
| Q50 | C | `math.pi≈3.14>3` 為 True；`ceil(4.1)=5`（向上取整）。 |

> ✅ 本卷所有程式輸出題已用 Python 直譯器實跑驗證，答案正確。
