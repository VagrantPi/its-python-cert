# ITS Python 證照｜模擬題庫（Q101–Q168，補齊各領域至均衡）

> 為平衡各領域題數，補充 Q101–Q168 共 68 題，使六大領域各達 **28 題**，**題庫合計 168 題**。
> 所有含程式輸出／例外型別的題目均經 **Python 直譯器實跑驗證**（非推測）。

## 補題後各領域題數

| 領域 | 原題數 | 補充 | 現題數 |
|------|:----:|:----:|:----:|
| D1 資料型別與運算子 | 24 | +4（Q101–104） | 28 |
| D2 流程控制 | 28 | +0 | 28 |
| D3 輸入與輸出 | 21 | +7（Q105–111） | 28 |
| D4 函式與程式結構 | 12 | +16（Q112–127） | 28 |
| D5 除錯與錯誤處理 | 8 | +20（Q128–147） | 28 |
| D6 模組與工具 | 7 | +21（Q148–168） | 28 |
| **合計** | 100 | +68 | **168** |

---

## 領域 1：資料型別與運算子（Q101–Q104）

**Q101.** `print(abs(-7))` → ?　(A) `7` (B) `-7` (C) `0` (D) 錯誤
**Q102.** `print(round(3.14159, 2))` → ?　(A) `3.14` (B) `3.1` (C) `3.142` (D) `3`
**Q103.** `print(5 == 5.0, 5 != "5")` → ?　(A) `True True` (B) `False True` (C) `True False` (D) `False False`
**Q104.** `print("Hello"[0], "Hello"[-1])` → ?　(A) `H o` (B) `H l` (C) `He o` (D) `P n`

## 領域 3：輸入與輸出（Q105–Q111）

**Q105.** `print("A", end=" ")` 後接 `print("B")`，輸出為？　(A) `A B`（同一行） (B) `A` 換行 `B` (C) `AB` (D) `A  B`
**Q106.** `print(3, 4, 5, sep="+")` → ?　(A) `3 4 5` (B) `3+4+5` (C) `345` (D) `12`
**Q107.** 輸入 `hello`，`x = input(); print(x.upper())` → ?　(A) `hello` (B) `HELLO` (C) `Hello` (D) 錯誤
**Q108.** 開檔模式 `"w"`：若檔案已存在，會？　(A) 附加到末端 (B) 清空原內容重新寫入 (C) 唯讀不可寫 (D) 引發錯誤
**Q109.** `with open("f.txt", "r") as f:` 區塊結束後，檔案狀態？　(A) 仍開啟需手動 close (B) 自動關閉 (C) 被刪除 (D) 被清空
**Q110.** `readline()` 與 `readlines()` 的差異？　(A) 兩者相同 (B) 前者讀一行(字串)、後者讀全部(串列) (C) 前者讀全部、後者讀一行 (D) 後者回傳整數
**Q111.** 開檔模式 `"x"`（獨佔建立），當檔案已存在時？　(A) 覆寫 (B) 附加 (C) 引發 `FileExistsError` (D) 自動改名

## 領域 4：函式與程式結構（Q112–Q127）

**Q112.** `def add(*a): return sum(a)`；`print(add(1,2,3,4,5))` → ?　(A) `15` (B) `12345` (C) `5` (D) 錯誤
**Q113.** `def f(x): return x+1`；`print(f(f(f(0))))` → ?　(A) `1` (B) `2` (C) `3` (D) `0`
**Q114.** `def g(a,b): return a-b`；`print(g(b=3, a=10))` → ?　(A) `-7` (B) `7` (C) `13` (D) 錯誤
**Q115.** `def h(): return 1, 2`；`print(h())` → ?　(A) `1 2` (B) `(1, 2)` (C) `[1, 2]` (D) `12`
**Q116.**
```python
def k(n):
    if n <= 1:
        return 1
    return n * k(n - 1)
print(k(4))
```
→ ?　(A) `24` (B) `12` (C) `4` (D) `10`
**Q117.** `x = "global"`；`def f(): x = "local"`；`f(); print(x)` → ?　(A) `local` (B) `global` (C) 錯誤 (D) `None`
**Q118.** `def f(a, b=2, c=3): return a+b+c`；`print(f(1, c=10))` → ?　(A) `6` (B) `13` (C) `14` (D) 錯誤
**Q119.**
```python
def outer():
    def inner():
        return 5
    return inner()
print(outer())
```
→ ?　(A) `5` (B) `inner` (C) `None` (D) 錯誤
**Q120.** `def area(w, h): return w * h`；`print(area(h=3, w=4))` → ?　(A) `7` (B) `12` (C) `43` (D) 錯誤
**Q121.** `def f(): return`；`print(f())` → ?　(A) `0` (B) 空白 (C) `None` (D) 錯誤
**Q122.**
```python
def total(lst):
    s = 0
    for n in lst:
        s += n
    return s
print(total([1, 2, 3, 4]))
```
→ ?　(A) `10` (B) `4` (C) `1234` (D) `24`
**Q123.** `def greet(name="World"): return "Hello, " + name`；`print(greet())` → ?　(A) `Hello, name` (B) `Hello, World` (C) `Hello, ` (D) 錯誤
**Q124.** `def swap(a, b): return b, a`；`x, y = swap(1, 2); print(x, y)` → ?　(A) `1 2` (B) `2 1` (C) `(2, 1)` (D) 錯誤
**Q125.** `def f(a, b, c): return a*100 + b*10 + c`；`print(f(1, 2, 3))` → ?　(A) `6` (B) `123` (C) `15` (D) `132`
**Q126.** `count = 10`；`def show(): return count + 5`；`print(show())` → ?　(A) `5` (B) `15` (C) 錯誤(NameError) (D) `None`
**Q127.** `def power(base, exp=2): return base ** exp`；`print(power(3), power(3, 3))` → ?　(A) `9 27` (B) `6 9` (C) `9 9` (D) `27 27`

## 領域 5：除錯與錯誤處理（Q128–Q147）

**Q128.** `try: print(int("10")) except: print("e")` → ?　(A) `10` (B) `e` (C) `"10"` (D) 錯誤
**Q129.**
```python
try:
    y = [1, 2]
    print(y[10])
except IndexError:
    print("idx")
```
→ ?　(A) `idx` (B) `2` (C) 崩潰 (D) 無輸出
**Q130.** 存取字典不存在的鍵 `{"a":1}["b"]`（在 try/except KeyError 中印 `key`）→ ?　(A) `key` (B) `1` (C) 崩潰 (D) `None`
**Q131.** `try: print(1/0) except ZeroDivisionError: print("zero") finally: print("end")` → ?　(A) `zero` (B) `zero` 換行 `end` (C) `end` (D) 崩潰
**Q132.** `"2" + 2` 會引發哪種例外？　(A) `ValueError` (B) `TypeError` (C) `SyntaxError` (D) 無錯誤
**Q133.** `int("3.5")` 的結果？　(A) `3` (B) `3.5` (C) `ValueError` (D) `TypeError`
**Q134.** 存取未定義的變數 `undefined_var` 會引發？　(A) `ValueError` (B) `NameError` (C) `KeyError` (D) 無錯誤
**Q135.**
```python
try:
    n = int("abc")
except ValueError:
    n = 0
except:
    n = -1
print(n)
```
→ ?　(A) `0` (B) `-1` (C) `abc` (D) 崩潰
**Q136.** `try: raise ValueError("bad") except ValueError as e: print(e)` → ?　(A) `ValueError` (B) `bad` (C) `e` (D) 無輸出
**Q137.**
```python
try:
    print("try")
except:
    print("except")
else:
    print("else")
```
→ ?　(A) `try` (B) `try` 換行 `else` (C) `try` 換行 `except` (D) `else`
**Q138.** `try: print(1) finally: print(2)` → ?　(A) 只有 `1` (B) `1` 換行 `2` (C) 只有 `2` (D) 錯誤
**Q139.** `m = [[1,2],[3,4]]; m[2][0]`（在 try/except IndexError 中印 `oob`）→ ?　(A) `oob` (B) `3` (C) `1` (D) 崩潰
**Q140.** `try: assert 1 == 2, "no" except AssertionError: print("failed")` → ?　(A) `no` (B) `failed` (C) `True` (D) 無輸出
**Q141.** `len(None)` 會？　(A) 回傳 `0` (B) `TypeError` (C) `ValueError` (D) 回傳 `None`
**Q142.** `"abc".index("z")` 會？　(A) 回傳 `-1` (B) `ValueError` (C) `KeyError` (D) 回傳 `None`
**Q143.** `[1, 2, 3].remove(9)` 會？　(A) 無事發生 (B) `ValueError` (C) `IndexError` (D) 回傳 `False`
**Q144.** `{"x": 1}.pop("y")` 會？　(A) 回傳 `None` (B) `KeyError` (C) `ValueError` (D) 無事發生
**Q145.** `float("abc")` 會？　(A) `0.0` (B) `ValueError` (C) `TypeError` (D) `None`
**Q146.** `try: result = 10 + "5" except TypeError: result = "型別錯誤"`；`print(result)` → ?　(A) `15` (B) `105` (C) `型別錯誤` (D) 崩潰
**Q147.**
```python
try:
    x = [1][5]
except KeyError:
    print("k")
except IndexError:
    print("i")
```
→ ?　(A) `k` (B) `i` (C) 兩者都印 (D) 崩潰

## 領域 6：模組與工具（Q148–Q168）

**Q148.** `import math; print(math.factorial(5))` → ?　(A) `120` (B) `25` (C) `15` (D) `5`
**Q149.** `print(math.floor(2.9), math.ceil(2.1))` → ?　(A) `2 3` (B) `3 2` (C) `3 3` (D) `2 2`
**Q150.** `print(math.fabs(-4.5))` → ?　(A) `-4.5` (B) `4.5` (C) `4` (D) `5`
**Q151.** `print(math.gcd(18, 24))` → ?　(A) `6` (B) `12` (C) `2` (D) `72`
**Q152.** `print(round(math.pi, 3))` → ?　(A) `3.14` (B) `3.142` (C) `3.141` (D) `3.1416`
**Q153.** `print(math.pow(3, 2))` → ?　(A) `9` (B) `9.0` (C) `6` (D) `8`
**Q154.** `print(math.sqrt(81))` → ?　(A) `9` (B) `9.0` (C) `81` (D) 錯誤
**Q155.** `print(math.trunc(3.99))` → ?　(A) `4` (B) `3` (C) `3.9` (D) `3.99`
**Q156.** `from math import pi; print(int(pi))` → ?　(A) `3` (B) `4` (C) `3.14` (D) 錯誤
**Q157.** `import random; print(random.randint(5, 5))` → ?　(A) `0` (B) `5` (C) 隨機 (D) 錯誤
**Q158.** `print(random.choice(["only"]))` → ?　(A) `only` (B) 隨機 (C) `["only"]` (D) 錯誤
**Q159.** `nums = [7]; print(random.choice(nums))` → ?　(A) `0` (B) `7` (C) 隨機 (D) 錯誤
**Q160.** `from random import randrange; print(randrange(1, 2))` → ?　(A) `1` (B) `2` (C) 隨機 1 或 2 (D) 錯誤
**Q161.** `print(math.isqrt(17))` → ?　(A) `4` (B) `5` (C) `4.12` (D) `8`
**Q162.** 只匯入 `math` 的 `sqrt` 函式，正確寫法為？　(A) `from math import sqrt` (B) `import sqrt from math` (C) `import math.sqrt` (D) `using math.sqrt`
**Q163.** `print(math.ceil(-1.2))` → ?　(A) `-1` (B) `-2` (C) `1` (D) `2`
**Q164.** `print(math.floor(-1.2))` → ?　(A) `-1` (B) `-2` (C) `1` (D) 錯誤
**Q165.** `import datetime; d = datetime.date(2024, 7, 1); print(d.year)` → ?　(A) `2024` (B) `7` (C) `1` (D) 錯誤
**Q166.** `from datetime import date; d = date(2024, 12, 25); print(d.month)` → ?　(A) `25` (B) `12` (C) `2024` (D) 錯誤
**Q167.** `print(math.pow(5, 0))` → ?　(A) `0` (B) `1` (C) `1.0` (D) `5`
**Q168.** `import math; print(2 ** 0.5 == math.sqrt(2))` → ?　(A) `True` (B) `False` (C) 錯誤 (D) `1.414`

---

## 解析與答案（全部經 Python 實跑驗證）

| 題 | 答 | 解析 |
|:--:|:--:|------|
| Q101 | A | `abs()` 取絕對值。 |
| Q102 | A | `round(x, 2)` 四捨五入到小數 2 位。 |
| Q103 | A | `5==5.0` 為 True；`5!="5"`（整數≠字串）為 True。 |
| Q104 | A | 索引 0 為 `H`、-1 為最後字元 `o`。 |
| Q105 | A | `end=" "` 不換行，接著印 B → `A B` 同一行。 |
| Q106 | B | `sep="+"` 以加號分隔。 |
| Q107 | B | `.upper()` 轉大寫。 |
| Q108 | B | `"w"` 會清空(truncate)原內容再寫入。 |
| Q109 | B | `with` 區塊結束自動關檔。 |
| Q110 | B | readline 讀一行(字串)、readlines 讀全部(串列)。 |
| Q111 | C | `"x"` 檔案已存在會引發 `FileExistsError`。 |
| Q112 | A | `*a` 收集為 tuple，`sum` = 15。 |
| Q113 | C | 0→1→2→3。 |
| Q114 | B | 關鍵字引數對應 a=10,b=3 → 7。 |
| Q115 | B | 回傳多值為 tuple `(1, 2)`。 |
| Q116 | A | 遞迴階乘 4! = 24。 |
| Q117 | B | 函式內指派為區域變數，不影響全域。 |
| Q118 | B | b 用預設 2、c=10 → 1+2+10=13。 |
| Q119 | A | 巢狀函式呼叫 inner 回傳 5。 |
| Q120 | B | 關鍵字引數 w=4,h=3 → 12。 |
| Q121 | C | 空 `return` 回傳 `None`。 |
| Q122 | A | 累加 1+2+3+4=10。 |
| Q123 | B | 使用預設參數 name="World"。 |
| Q124 | B | 回傳 `b, a` 拆解為 x=2,y=1。 |
| Q125 | B | 1×100+2×10+3=123。 |
| Q126 | B | 函式可**讀取**全域變數 → 15。 |
| Q127 | A | 3²=9；3³=27。 |
| Q128 | A | `int("10")=10` 無例外。 |
| Q129 | A | 索引越界被 IndexError 攔截。 |
| Q130 | A | 存取不存在的鍵被 KeyError 攔截。 |
| Q131 | B | 除以 0 印 `zero`，finally 一定印 `end`。 |
| Q132 | B | 字串與整數不能相加 → `TypeError`。 |
| Q133 | C | 含小數點字串無法直接轉 int → `ValueError`。 |
| Q134 | B | 未定義變數 → `NameError`。 |
| Q135 | A | `ValueError` 先被對應的 except 攔截，n=0。 |
| Q136 | B | `as e` 取得例外物件，`print(e)` 印訊息 `bad`。 |
| Q137 | B | 無例外時 `else` 會執行 → `try` 換行 `else`。 |
| Q138 | B | `finally` 一定執行 → `1` 換行 `2`。 |
| Q139 | A | `m[2]` 索引越界 → IndexError 被攔截。 |
| Q140 | B | 斷言失敗 → `AssertionError` 被攔截。 |
| Q141 | B | `None` 沒有長度 → `TypeError`。 |
| Q142 | B | `index` 找不到子字串 → `ValueError`（`find` 才回 -1）。 |
| Q143 | B | `remove` 不存在的值 → `ValueError`。 |
| Q144 | B | `pop` 不存在的鍵且無預設 → `KeyError`。 |
| Q145 | B | 非數字字串轉 float → `ValueError`。 |
| Q146 | C | 型別不合被 TypeError 攔截，result 設為字串。 |
| Q147 | B | 清單越界是 IndexError，由對應 except 攔截。 |
| Q148 | A | 5! = 120。 |
| Q149 | A | floor(2.9)=2、ceil(2.1)=3。 |
| Q150 | B | `fabs` 取絕對值（回傳 float 4.5）。 |
| Q151 | A | 18 與 24 的最大公因數 6。 |
| Q152 | B | round(π,3)=3.142。 |
| Q153 | B | `math.pow` 回傳 float → 9.0。 |
| Q154 | B | `sqrt(81)=9.0`（float）。 |
| Q155 | B | `trunc` 截斷小數 → 3。 |
| Q156 | A | `int(3.14159...)=3`。 |
| Q157 | B | 上下限相同必為 5。 |
| Q158 | A | 單一元素清單必選中它。 |
| Q159 | B | 同上，必為 7。 |
| Q160 | A | `randrange(1,2)` 不含上界，只有 1 可選。 |
| Q161 | A | `isqrt(17)=4`（整數平方根，向下）。 |
| Q162 | A | `from math import sqrt`。 |
| Q163 | A | `ceil(-1.2)=-1`（往 +∞）。 |
| Q164 | B | `floor(-1.2)=-2`（往 -∞）。 |
| Q165 | A | `date(2024,7,1).year=2024`。 |
| Q166 | B | `.month=12`。 |
| Q167 | C | 任何數 0 次方為 1；`math.pow` 回 float → 1.0。 |
| Q168 | A | 兩種算平方根方式結果相同 → True。 |

> ✅ 本卷所有答案（含例外型別、math/random/datetime 回傳值）皆以 Python 直譯器實跑核對。
